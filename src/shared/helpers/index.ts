/**
 * Shared helpers — re-exports from established locations so that
 * future code can import from a single `@/shared/helpers` entry point.
 */

export { normalizeIranianPhone } from "@/lib/phone";
export { cn } from "@/lib/utils";
